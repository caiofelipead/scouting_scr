# ============================================
# CONFIGURAÇÕES - Scout Pro API
# ============================================

# Banco de Dados PostgreSQL (Railway)
# 🚂 IMPORTANTE: Copie a DATABASE_URL do Railway!
# 1. Acesse o serviço Postgres no Railway
# 2. Vá em Variables → DATABASE_URL (clique para revelar)
# 3. Cole aqui o valor completo
# Formato: postgresql://PGUSER:PGPASSWORD@PGHOST:PGPORT/PGDATABASE
DATABASE_URL=postgresql://postgres:senha@containers-us-west-xxx.railway.app:5432/railway

# Ou use DATABASE_PUBLIC_URL se quiser acesso externo
# DATABASE_PUBLIC_URL=postgresql://postgres:senha@proxy.rlwy.net:12345/railway

# Configurações do Pool de Conexões (otimizado para Railway)
DB_POOL_SIZE=15
DB_MAX_OVERFLOW=5
DB_POOL_RECYCLE=3600

# JWT Authentication
# 🔐 GERE UMA CHAVE FORTE! Execute: openssl rand -hex 32
SECRET_KEY=your-super-secret-key-change-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS - Frontend URLs (adicione seu domínio Vercel aqui)
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","https://seu-frontend.vercel.app"]

# Aplicação
APP_NAME=Scout Pro API
APP_VERSION=1.0.0
DEBUG=False

# Paginação
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=200

# Upload de Fotos (compatível com sistema atual transfermarkt_id)
UPLOAD_DIR=fotos
MAX_UPLOAD_SIZE=5242880
ALLOWED_EXTENSIONS=["jpg","jpeg","png","webp"]

# Redis (opcional - cache)
REDIS_URL=

# Google Sheets (sincronização)
GOOGLE_CREDENTIALS_PATH=
SPREADSHEET_ID=
